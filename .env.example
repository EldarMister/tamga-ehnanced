# URL развёрнутого FastAPI бэкенда. Подставляется на этапе сборки (vite build).
# Если фронт и бэк на одном домене (через reverse proxy) — оставь пустым.
VITE_API_BASE=https://tamga-backend.up.railway.app
